fun main() {
    var meuCarro = Carro("Supra", "Toyota", 1993, 1000f)
    meuCarro.mostrarDados()

    /*
    A diferença entre uma variável e um objeto é que o objeto é uma instância de uma classe, poderiamos dar um exemplo de uma forma de biscoitos, neste exemplo a classe seria a forma,
     que define como serão os biscoitos, e o objeto seria o biscoito em si.

    Já as variáveis são apenas uma maneira de armazenar um valor, como se fossem contêineres dentro da memória.
     */
}